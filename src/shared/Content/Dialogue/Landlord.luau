-- เจ้าของห้องเช่า (เฟส 1 + เฟส 2) — ตัวแทนค่าครองชีพ ผูกกับ Bad End ① (โดนไล่ที่)
-- ⚠️ [placeholder] — รอบทจริง
return {
	p1 = { "[placeholder] เจ้าของห้อง เฟส 1 — ทวงค่าเช่า" },
	p2 = { "[placeholder] เจ้าของห้อง เฟส 2 — เห็นเราเริ่มดังแล้ว" },
}
