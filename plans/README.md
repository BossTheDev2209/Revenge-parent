# plans/

หนึ่งไฟล์ต่อหนึ่ง feature ตาม superpowers workflow (brainstorm → plan → TDD → review)

ตั้งชื่อ: `NNN-slug.md` เช่น `001-follower-service.md`

ทุก task ในแผนต้องมีครบ 4 อย่าง ไม่งั้นยังไม่ใช่แผน:
1. path ไฟล์ที่จะแก้ (เป๊ะ)
2. โค้ดที่จะเขียน (จริง ไม่ใช่ pseudocode)
3. วิธี verify
4. success criteria

ขนาด task: 2–5 นาที ถ้าใหญ่กว่านั้นให้ซอย

## คิวตาม design doc §11 (implement ที่ต้องเขียน)

- [ ] 001 Follower service — base × VidQ × mental + viral
- [ ] 002 Money service — follower × rate + rent รายสัปดาห์
- [ ] 003 Mental service — 3 โซน + drain/recovery + activity cooldown
- [ ] 004 Phase gate — อ่าน follower → คืนเฟส
- [ ] 005 Dialogue engine — typewriter + SFX + speed tag `[slow]`/`[fast]`
- [ ] 006 Calendar + sponsor + schedule event
- [ ] 007 Ending resolver — priority order, อ่าน VidQ % + comment %
