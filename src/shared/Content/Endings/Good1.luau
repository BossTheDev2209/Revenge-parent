-- Ending Good1 — cutscene script (design doc §5, §8)
-- step: {type="text", text=...} | {type="wait", t=วินาที} | {type="camera", pos={x,y,z}, look={x,y,z}}
-- ⚠️ [placeholder] — รอ storyboard จริงจากทีม แก้ list นี้ได้เลย
return {
	{ type = "text", text = "[slow](ถึง 1M ด้วยใจดี พ่อแม่ยอมรับ ครอบครัวกลับมาอบอุ่น)" },
	{ type = "wait", t = 2 },
	{ type = "text", text = "[placeholder ending Good1 — ทีมเขียน storyboard จริงแทนตรงนี้]" },
}
