-- บทแม่ — ทางเดียว typewriter (design doc §7) ไม่มี choice
-- แต่ละบรรทัด = 1 กล่องข้อความ กด/คลิกเพื่อไปต่อ
-- tag คุมความเร็วกลางบรรทัด: [slow] [fast] [normal]
-- ⚠️ [placeholder] — ทีมเขียนบทจริงแทนได้เลย แค่แก้ข้อความใน list นี้
return {
	"แม่: กลับมาทำไม... [slow]ไม่มีเงินใช่ไหมล่ะ",
	"แม่: [fast]บอกกี่ครั้งแล้ว! [normal]อาชีพแบบนี้มันไม่มั่นคง",
}
