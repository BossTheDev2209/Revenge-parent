-- PhaseTransitions — ข้อความจอดำตอนข้ามเฟส (follower ทะลุ gate → วาปเข้าแมพเฟสใหม่)
-- key = เลขเฟสที่กำลังจะเข้า · ค่า = บรรทัดข้อความบนจอดำ (reuse DialogueUI typewriter + คลิกต่อ)
-- ว่าง/nil = จอดำเฉยๆ แล้ววาป ไม่ error · [placeholder] Boss เขียนบทจริงเอง แก้แค่ list นี้
-- เข้าเฟส 2 = ทะลุ 10K follower · เข้าเฟส 3 = ทะลุ 100K (docs/02 §1)
return {
	[2] = { "[placeholder] ข้อความตอนย้ายเข้าแมพเฟส 2 — เขียนบทจริงแทน" },
	[3] = { "[placeholder] ข้อความตอนย้ายเข้าแมพเฟส 3 — เขียนบทจริงแทน" },
}
