-- UIAssets.luau — รูปทุกชิ้นของ UI อยู่ที่นี่ที่เดียว
-- ★ ทีมวาดรูปเสร็จ: อัปโหลดเป็น Decal/Image ใน Roblox → เอาเลข asset id มาใส่ เช่น "rbxassetid://123456"
-- ปล่อย "" = โค้ดใช้ตัวหนังสือ/emoji แทนอัตโนมัติ (เกมไม่พังระหว่างรอรูป)
-- ขนาดแนะนำอยู่ท้ายบรรทัด — PNG พื้นหลังใส
return {
	-- ===== icon แอปบน desktop (สี่เหลี่ยมจัตุรัส 128×128) =====
	app_edit = "",     -- ตัดคลิป (กรรไกร/filmstrip)
	app_upload = "",   -- อัปโหลด (ลูกศรขึ้น/เมฆ)
	app_feedback = "", -- feedback (กราฟ/กล่องคอมเมนต์)
	app_bank = "",     -- ธนาคาร (กระปุก/ธนบัตร)
	app_calendar = "", -- ปฏิทิน
	app_shop = "",     -- ร้านค้า (รถเข็น)
	app_message = "",  -- ข้อความ (ซองจดหมาย/บับเบิล)
	app_manage = "",   -- จ้างพนักงาน (คนสองคน)

	-- ===== wallpaper ต่อเฟส (1024×640 — สัดส่วนจอ 4:2.6) =====
	wallpaper_phase1 = "", -- โน้ตบุ๊คมือสอง: จอเก่าๆ สีหม่น
	wallpaper_phase2 = "", -- desktop: ฟ้าสดใสแบบ ref Jim's Computer
	wallpaper_phase3 = "", -- เครื่องเทพ: ดาร์ค+neon

	-- ===== HUD ติดจอ (64×64) =====
	hud_follower = "", -- คนตัวเล็ก
	hud_money = "",    -- เหรียญ/แบงก์
	hud_clip = "",     -- ม้วนฟิล์ม
	hud_camera = "",   -- กล้อง
	hud_mental = "",   -- หัวใจ (บนหลอดใจ)
	hud_save = "",     -- แผ่นดิสก์ (ปุ่มเซฟ)

	-- ===== อื่นๆ =====
	btn_exit = "",     -- ปุ่มปิดเครื่อง (power icon, 64×64)
	cursor = "",       -- เคอร์เซอร์ในจอ (ถ้าอยากมี, 32×32)
}
