-- DMs.luau — ข้อความ DM ในแอป message (design doc §6: script ตายตัว **ไม่มีผลต่อ ending**)
-- ⚠️ [placeholder] — ทีมเติม DM จริงได้เลย เพิ่มก้อนใหม่ต่อท้าย — วิธีเขียนเต็มๆ ดู docs/10-team-roles.md
-- 2 format:
--   แบบง่าย (NPC พูดฝ่ายเดียว):  { from="ชื่อ", lines={"บรรทัด 1", "บรรทัด 2"} }
--   แบบมีช้อย (เลือกได้แต่ NPC ตอบเหมือนเดิมทุกช้อย):
--   { from="ชื่อ", exchanges={
--       { npc="NPC พูด", choices={"ช้อย 1", "ช้อย 2", "ช้อย 3"}, reply="NPC ตอบกลับ" },
--   } }
return {
	{ from = "พนักงาน 1", lines = { "พี่ครับ เงินเดือนออกวันไหนครับ 🙏", "เดือนนี้ผมขอเบิกก่อนได้ปะ" } },
	{ from = "Sponsor", exchanges = {
		{ npc = "สวัสดีครับ สนใจรับงานรีวิวสินค้าไหมครับ", choices = { "สนใจครับ!", "ขอคิดดูก่อน", "ไม่สนใจ" }, reply = "โอเคครับ รายละเอียดงานเดี๋ยวส่งให้ทางนี้นะครับ [placeholder]" },
	} },
}
