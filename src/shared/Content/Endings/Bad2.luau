-- Ending Bad2 — cutscene script (docs/02 §5, §8, §9.4)
-- step: {type="text", text=...} | {type="wait", t=วินาที} | {type="camera", cam="ชื่อ Part ใน Workspace.CutsceneCams"}
-- ⚠️ [placeholder] — รอ storyboard จริง แก้ list นี้ได้เลย
return {
	{ type = "text", text = "[placeholder ending Bad2]" },
	{ type = "wait", t = 2 },
}
